// you can tell IboView to load and execute a specific script file automatically
// on every startup under Edit/Preferences. This can be used to change global
// options.

// For example, on my PCs I increase the default number of depth peeling layers
// (this makes rendering slower, but increases the maximum complexity of
// intersecting transparent surfaces which are rendered correctly).
view.depth_peeling_layers = 8;

// Other things you might want to change could be atom sizes/colors (see
// (change_atom_or_element_properties.js) or anything else you can modify in
// scripts.

